// async componentDidUpdate(prevProps, prevState) {
//   if (prevProps.match.params.user !== this.props.match.params.user) {
//     const posts = await this.fetchPosts(this.state.users);
//     this.setState({ posts });
//   }
// }
